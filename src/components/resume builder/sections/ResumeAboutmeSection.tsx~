import ResumeSection from "./ResumeSection.tsx";
import { useResumeBuilder } from "../../../context/ResumeBuilderContext.tsx";

function ResumeAboutmeSection() {
  const { resumeInfo } = useResumeBuilder();

  return (
    <ResumeSection
      title="About me"
      titles={[resumeInfo.personal.aboutMe.slice(0, 50)]}
    />
  );
}

export default ResumeAboutmeSection;
